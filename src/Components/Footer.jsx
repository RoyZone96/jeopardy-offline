export default function Footer() {
    return (
            <footer className="app-footer">
            Local boards are stored in your browser's localStorage under the app
            key.
          </footer>
    );
}